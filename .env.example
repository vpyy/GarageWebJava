# ============================================================
# GarageGo — .env.example (template an toàn để commit)
# Copy file này thành .env rồi điền giá trị thực
# ============================================================

# --- MySQL ---
DB_PASSWORD=your_strong_password_here

# --- JWT ---
# Generate bằng: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_here
